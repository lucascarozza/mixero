import styles from "./ManagePlaylists.module.css";

const ManagePlaylists = () => {
  return (
    <section className={styles.contentWrapper}>
      <h2 className={styles.pageTitle}>Manage Playlists</h2>
    </section>
  );
};

export default ManagePlaylists;